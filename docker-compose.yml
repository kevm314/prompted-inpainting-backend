version: "3.7"

services:
  backend:
    image: inpainting-backend
    # debug only
    # build:
    #   network: host
    #network_mode: host
    ports:
      - 8000:8000
    env_file:
      - app/dev.env
    command: uvicorn main:app --host 0.0.0.0 --port 8000
    

  frontend:
    image: inpainting-frontend
    # debug only
    # build:
    #   network: host
    #network_mode: host
    ports:
      - 3000:3000

# networks:
#   default:
#     external: true
#     name: host